Bugs:

 * AppImage sample cuts at the beginning on first sound

Awaiting implementation:

 * wybór backendu audio z listy
 * nie powtarzać "obraz w kieszeni" (inne?)
 * about
 * keyboard shortcuts
 * hover help? pytajnik-help?
 * przerwania - (?) hover poprawić tekst

Requires more emu work:

 * MODE/STEP
 * STOP*N
 * LEDs PWM

To investigate, open questions:

 * symbols (loading, saving, in-dasm usage)
 * memview: load from address (jak load OS, zamiast load OS?)
 * save memory: menu? memview?
 * background sound
 * help? (?) on more widgets?
 * details on mouseover? (dec/hex/oct/bin/field decodes) - sr, r0, rz, memview

Potential, for the future:

 * log manipulation through a dedicated dock?
 * commandline - only if it proves genuinely superior to using existing UI elements
 * micro-panel
 * dasm syntax highlight
 * zapamiętywanie pozycji kluczy?
