* commandline 'eats' endings when using history sometimes
* debuger: mem for blocks other than 0
* debuger: treating filenames/texts properly
* when accessing memory with pointers, check ranges
* cpu: floating point instructions
* debuger: save memory image
* debuger: breakpoints
* debuger: operator panel view (?)
* debuger: step should work the same way as CYCLE (cycle on instruction and interrupt)
* autotests
* other: commandline dasm/trans (?)
* separate I/O thread
