* cpu: registers abstraction (as for memory)
* cpu: move registers to an array (for debuger)
* cpu: disallow writes to r0 upper byte
* cpu: floating point instructions
* debuger: save memory image
* debuger: setting registers
* debuger: setting memory
* debuger: save memory image
* debuger: breakpoints
* debuger: operator panel view
* debuger: step should work the same way as CYCLE (cycle on instruction and interrupt)
* autotests
* other: commandline dasm/trans (?)
* other: move dasm/trans things away from iset (?)
