where to store own configuration?
shortcuts
breakpoints
memory configuration view
details on mouseover (interrupts, int mask, nb, q, bs, dasm, ...)
- int/int mask display in the status bar
+ flags/p/q/bs/nb diplay in the status bar
setting breakpoints from dasm view
setting IC from dasm view
memory view
editing memory contents
color schemes (own/system)
dasm view - minimal syntax highlight
dasm view - marking position(-s)?
control panel
switching between views (cp/debugger/cp+debugger)
delegating error display to the UI
translation
watches?
stack?
