Bugs:


CPU:

* floating point instructions
* interrupts
* hlt

Debuger:

* memory/register traces
* macro for accessing interrupt states
* save memory image
* load/save snapshot (?)
* step should work the same way as CYCLE (cycle on instruction and interrupt) (?)

Other:

* fix source directory structure, naming
* autotools
* autotests
* separate I/O thread
* operator panel view (?)
* commandline dasm/trans (?)

